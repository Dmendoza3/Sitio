*v-if: to hide messages

*var app = new Vue({
  el: '#app',
  data: {
    message: 'Hello Vue!'
  }
}) : to show messages

* <li v-for="todo in todos">
      {{ todo.text }}
    </li>
    : for adding new message

*v-on: input for messages